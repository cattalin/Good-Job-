import { TestBed, inject } from '@angular/core/testing';

import { SearchFeedComponent } from './search-feed.component';

describe('a search-feed component', () => {
	/*let component: SearchFeedComponent;

	// register all needed dependencies
	beforeEach(() => {
		TestBed.configureTestingModule({
			providers: [
				SearchFeedComponent
			]
		});
	});

	// instantiation through framework injection
	beforeEach(inject([SearchFeedComponent], (SearchFeedComponent) => {
		component = SearchFeedComponent;
	}));

	it('should have an instance', () => {
		expect(component).toBeDefined();
	});*/
});